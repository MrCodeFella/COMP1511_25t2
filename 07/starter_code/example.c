int number = 42;      // house (number) with value 42
int *ptr = &number;   // note (ptr) with the address of that house
*ptr = 100;           // go to the house pointed by ptr, and update value to 100