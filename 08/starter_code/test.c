
struct node {
    int data;
    struct node *next;
}

int main(void) {
    // = malloc(sizeof(struct my_struct));
    struct node *node1 = malloc(sizeof(struct node));
    my_array[5] = 'c';

    
    free(my_array);

    return 0;
}